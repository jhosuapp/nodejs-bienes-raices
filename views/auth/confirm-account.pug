extends ../layout/index

block content
    section.container.container--center
        article.ctm-fonts.container-form.ctm-fonts--center
            h1=page

            form.form-general(method="POST" action="/auth/register" noValidate)
                if(successful)
                    div.success.success-verify-account
                        img(src="/images/svg/check.svg")
                        p=description
                        a(href="/auth/login").btn Iniciar sesión
                if(!successful)
                    div.success.success-verify-account
                        img(src="/images/svg/error.svg")
                        p=description
                        a(href="/auth/register").btn Crear cuenta